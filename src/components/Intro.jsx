import { Typography, Container } from "@mui/material"

export default function Intro() {
    return (
        <Container>
            <Typography variant="h1" sx={{fontWeight: 'bold'}} >software developer, wellington.</Typography>
        </Container>
    )
}